import { getComponentsByTemplateDep } from '.'

// При написании тестов учитывать что вложенность нужно тестировать и по container тоже, не только по  repeater компоненте.

describe('getComponentsByTemplateDep', () => {
    it('...', () => {
        expect(getComponentsByTemplateDep()).toEqual([])
    })
})
