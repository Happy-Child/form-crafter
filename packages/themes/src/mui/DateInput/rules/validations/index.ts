import { isAdultRule } from './is-adult'
import { isDateRule } from './is-date'
import { maxDateRule } from './max-date'
import { minDateRule } from './min-date'

export const validations = {
    maxDateRule,
    minDateRule,
    isAdultRule,
    isDateRule,
}
